

class pawn extends piece {
  constructor(color) {
    super('pawn, color');
  }


  generateValidMoves(startX, startY) {
    const validMoves = [];

    return validMoves;
  }
}

module.exports = pawn;
