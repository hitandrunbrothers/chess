
class Piece {

  constructor(type, color) {
    this.type = type;
    this.color = color;
  }

  // set type (type) {
  //   this.type = type;
  // }

  // get type () {
  //   return this.type;
  // }

  // set color (color) {
  //   this.color = color;
  // }

  // get color () {
  //   return this.color;
  // }

  generateValidMoves(startX, startY) {
    console.log('move');
  }



}

module.exports = Piece;
